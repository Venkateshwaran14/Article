import { Byte } from "@angular/compiler/src/util";

export class Article {
    artileId: number=0;
    title: string='';
    content: string='';
    image: string='';
    articleStatusID: number=0;
    reviewerId: number=0;
}